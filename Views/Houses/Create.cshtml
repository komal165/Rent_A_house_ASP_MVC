@model Rent_A_House.BusinessLayer.House

@{
    ViewData["Title"] = "Create";
}

<h2>Create</h2>

<h4>House</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="HouseOwnerId" class="control-label"></label>
                <select asp-for="HouseOwnerId" class ="form-control" asp-items="ViewBag.HouseOwnerId"></select>
            </div>
            <div class="form-group">
                <label asp-for="HouseAddress" class="control-label"></label>
                <input asp-for="HouseAddress" class="form-control" />
                <span asp-validation-for="HouseAddress" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-outline-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
